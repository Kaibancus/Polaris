Attention dot + running strip refinements:
- New-message indicator is now a simple pulsing red dot only (numeric badge removed); it appears in step with the system taskbar's flash.
- Non-resident pinned apps now also show in the side dock's running strip while running.
- Unified the green "running" breathing dot size across the pinned column and the running strip (factor 0.07).
